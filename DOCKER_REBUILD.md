# 🐳 Docker Rebuild Инструкции

## Проблем с кеша
Docker кешира frontend build файловете в `dist/` папката. Когато правите промени в кода, трябва да rebuild-нете контейнерите.

## Бърз Rebuild (препоръчително)

```bash
# Спрете контейнерите
docker-compose down

# Rebuild само frontend без кеш
docker-compose build --no-cache frontend

# Стартирайте отново
docker-compose up -d
```

## Пълен Rebuild (ако горното не работи)

```bash
# Спрете и изтрийте всичко
docker-compose down -v

# Rebuild всички контейнери без кеш
docker-compose build --no-cache

# Стартирайте отново
docker-compose up -d
```

## Проверка на логове

```bash
# Вижте логовете на frontend
docker-compose logs -f frontend

# Вижте логовете на backend
docker-compose logs -f backend
```

## Бързи команди

```bash
# Рестарт на frontend
docker-compose restart frontend

# Влезте в frontend контейнера
docker-compose exec frontend sh

# Изтрийте всички Docker кешове (внимавайте!)
docker system prune -a
```

## Последни промени (2025)

### ✅ Нотификации за нови съобщения
- Badge с брой непрочетени съобщения в навигацията
- Червен кръг с номер на непрочетените съобщения
- Auto-refresh на всеки 5 секунди

### ✅ Подобрения в чата
- **Емоджи пикер** - бутон за добавяне на емоджита (😊 😢 😰 😡 ❤️ 👍 🙏 💪 🌟 ✨)
- **Важни съобщения** - маркиране със звезда ⭐
- **Подобрен UI** - по-модерен дизайн на съобщенията
- **Typing indicator** - "пише..." индикатор
- **Auto-scroll** - автоматично скролване до последното съобщение

### 📊 База данни промени
- Добавени колони `is_read` и `is_important` в `messages` таблицата
- Нови API endpoints:
  - `GET /api/chat/unread-count` - брой непрочетени съобщения
  - `POST /api/chat/toggle-important/:messageId` - маркиране като важно

## Важно! ⚠️
След всяка промяна в frontend кода, ВИНАГИ правете rebuild с `--no-cache` флаг!
